# 一家食光 · 前端开发记录

> 版本：v2.0-dev
> 更新日期：2026-06-13
> 当前阶段：从原型（一家食光.html）工程化为可开发的前端项目

---

## 一、现状评估

### 1.1 原有文件

| 文件 | 行数 | 用途 |
|------|------|------|
| `一家食光.html` | ~3896 行 | 完整原型：32 个 React 组件 + 设计令牌 + 内联样式 + Mock 数据，依赖 Babel Standalone 在浏览器端编译 JSX |
| `app-root.jsx` | ~196 行 | 设计画布画廊框架，仅含 P0-P3 四个屏幕 |
| `screen-p0~p3.jsx` | 各 ~200 行 | 4 个画廊展示用屏幕文件 |
| `shared.jsx` | ~212 行 | 画廊版共享组件（C、StatusBar、BottomSheet、Toast、Btn、AppInput、StepDots、NavBar） |

### 1.2 问题诊断

1. **单文件巨石**：3896 行 HTML 混合了样式、32 个组件、Mock 数据、路由逻辑，无法分工协作
2. **无模块化**：依赖 window 全局变量（`window.P10_Home = ...`）通信，组件间耦合混乱
3. **无状态管理**：数据流分散在 sessionStorage、window 临时变量、组件 local state 之间
4. **Mock 数据硬编码**：散落各处，无统一数据层
5. **缺少规格实现**：三份规格文档（家谱关系系统、Agent 对话 Skill、功能逻辑）中的核心算法均未在前端落地
6. **无构建工具**：依赖浏览器端 Babel 编译，无法使用 TypeScript、代码分割、Tree-shaking

---

## 二、规格文档解读

### 2.1 已有规格（3 份）

| 文档 | 核心内容 | 前端需实现 |
|------|---------|-----------|
| **家谱关系系统** v1.3 | 数据模型（family_member / family_relation / kinship_cache）、6种定位关系、堂/表判定算法、双向称谓推导、长幼排序 | 关系图数据结构、堂表判定函数、称谓推导函数、家谱树渲染接口 |
| **Agent 对话 Skill** v1.3 | AI 对话角色、信息抽取清单、对话策略、时间地点挖掘（必须到市级）、System Prompt 模板、故事生成输出结构 | 对话 UI、消息结构、状态追踪模型、上下文组装、草稿保存/恢复 |
| **功能逻辑整理版** | 点亮机制、抛钩子/还有人在等闭环、时间胶囊独立性、全选联动规则、图片4:3裁剪、AI调用场景 | 全选联动 Hook、时间胶囊逻辑、草稿自动保存、图片上传逻辑 |

### 2.2 设计系统（已有）

- 设计令牌（色彩、字体、间距、圆角、阴影）：`一家食光 设计系统.md`
- 页面交互清单（17 个主页面 + 子页面）：`uploads/一家食光_页面交互清单_1.md`

---

## 三、工程化目标架构

### 3.1 技术选型

| 层面 | 选择 | 理由 |
|------|------|------|
| 框架 | React 18 | 与原型一致，生产可用 |
| 语言 | TypeScript (渐进) | 核心数据模型、算法用 TS，组件先用 JSX |
| 状态管理 | React Context + useReducer | 无需引入 Redux，满足中型应用 |
| 样式方案 | CSS-in-JS (内联 → 提取) | 先保留内联风格，逐步提取为 CSS Modules |
| 构建工具 | Vite | 快速开发，支持 JSX/TSX/热更新 |
| Mock 数据 | 统一 mock/ 目录 | 结构化导出，替换散落硬编码 |
| 路由 | 自研 hash 路由 (保持) | 原型已有 workable 方案，增强即可 |

### 3.2 目录结构

```
一家食光/
├── index.html                 # 入口 HTML（保留 Babel Standalone 兼容）
├── package.json               # 待添加：Vite + React 依赖
├── vite.config.js             # 待添加
│
├── src/
│   ├── main.jsx               # 应用入口
│   ├── App.jsx                # 根组件（路由 + Context Provider）
│   │
│   ├── config/
│   │   ├── tokens.js          # 设计令牌 C（色彩/字体/间距）
│   │   └── routes.js          # 路由表 SCREENS 配置
│   │
│   ├── components/            # 共享 UI 组件
│   │   ├── ui/
│   │   │   ├── StatusBar.jsx
│   │   │   ├── Toast.jsx
│   │   │   ├── Btn.jsx
│   │   │   ├── AppInput.jsx
│   │   │   ├── BottomSheet.jsx
│   │   │   ├── NavBar.jsx
│   │   │   ├── StepDots.jsx
│   │   │   ├── LocationPicker.jsx
│   │   │   ├── ScrollPicker.jsx
│   │   │   └── BottomNav.jsx
│   │   │
│   │   └── layout/
│   │       └── PhoneFrame.jsx  # 手机外壳（390×844）
│   │
│   ├── pages/                 # 页面组件
│   │   ├── auth/              # 认证与入伙
│   │   │   ├── P0_Splash.jsx
│   │   │   ├── P1_Login.jsx
│   │   │   ├── P2_Family.jsx
│   │   │   └── P3_Position.jsx
│   │   ├── creation/          # 创作闭环
│   │   │   ├── P10_Home.jsx       # 味道桌
│   │   │   ├── P11_Anchor.jsx     # 记忆瞬间
│   │   │   ├── P30_Chat.jsx       # Agent 对话
│   │   │   └── P31_Story.jsx      # 故事生成
│   │   ├── family/            # 家族与社交
│   │   │   ├── P20_Tree.jsx       # 家谱
│   │   │   ├── P21_Person.jsx     # 人物详情
│   │   │   ├── P40_Feed.jsx       # 动态
│   │   │   └── P50_Me.jsx         # 我的
│   │   ├── heritage/          # 设置与传承
│   │   │   ├── P51_Settings.jsx
│   │   │   ├── P52_Family.jsx
│   │   │   ├── P60_Map.jsx
│   │   │   ├── P61_Book.jsx
│   │   │   └── P62_Capsule.jsx
│   │   └── sub/               # 内页
│   │       ├── P_EditProfile.jsx
│   │       ├── P_EditMember.jsx
│   │       ├── P_EditMemory.jsx
│   │       ├── P_EditFamily.jsx
│   │       ├── P_ConfirmMember.jsx
│   │       ├── P_Drafts.jsx
│   │       ├── P_GrayDish.jsx
│   │       ├── P_Notifications.jsx
│   │       ├── P_CreateFamily.jsx
│   │       ├── P_JoinFamily.jsx
│   │       ├── P_Register.jsx
│   │       ├── P_ForgotPwd.jsx
│   │       └── P_CapsuleDetail.jsx
│   │
│   ├── hooks/                 # 自定义 Hooks
│   │   ├── useApp.js          # App Context 消费
│   │   ├── useNavigation.js   # 路由导航
│   │   ├── useToast.js        # Toast 消息
│   │   ├── useFamily.js       # 家族数据
│   │   └── useMultiSelect.js  # 全选联动规则（§1.7）
│   │
│   ├── models/                # 数据模型（对齐三份规格）
│   │   ├── family-member.js   # family_member 表模型
│   │   ├── family-relation.js # family_relation 表模型
│   │   ├── kinship-cache.js   # kinship_cache 表模型
│   │   ├── conversation.js    # P30 对话消息结构
│   │   └── story-output.js    # P31 故事生成输出结构
│   │
│   ├── engine/                # 核心算法（规格落地）
│   │   ├── relation-graph.js  # 关系图构建与 BFS 路径查找
│   │   ├── kinship-derive.js  # 亲属称谓双向推导（§5）
│   │   ├── tang-biao.js       # 堂/表判定算法（§6）
│   │   ├── age-compare.js     # 长幼排序（§7）
│   │   └── label-to-edge.js   # 6 种定位关系 → 基础边转换（§14.2）
│   │
│   ├── context/               # React Context
│   │   ├── AppContext.jsx     # 全局上下文（navigate, showToast）
│   │   ├── FamilyContext.jsx  # 家族数据上下文
│   │   └── AuthContext.jsx    # 登录状态上下文
│   │
│   ├── mock/                  # Mock 数据（开发用）
│   │   ├── family-data.js     # 家族成员 + 关系边
│   │   ├── anchors.js         # 锚点（已点亮/灰锚点）
│   │   ├── capsules.js        # 时间胶囊
│   │   └── notifications.js  # 通知
│   │
│   └── utils/                 # 工具函数
│       ├── china-regions.js   # CN_REGIONS 省市数据
│       ├── date.js            # 日期格式化
│       └── storage.js         # sessionStorage 存取
│
├── public/                    # 静态资源
│   └── uploads/               # 图片资产（从 uploads/ 迁移）
│
└── specs/                     # 设计规格（软链接到现有 .md）
    └── (现有 5 份 .md 文件)
```

### 3.3 数据流架构

```
用户操作
  → Page Component (useState / useReducer)
    → Context (FamilyContext / AuthContext)
      → engine/ 算法模块 (kinship-derive, tang-biao)
        → Mock Data (开发) / API (生产)
      ← 计算结果回写 Context
    ← 组件重渲染
  ← UI 更新
```

---

## 四、实施计划

### Phase 1：基础设施 ✅ 已完成
- [x] 现状评估与架构方案
- [x] 提取设计令牌 `src/config/tokens.js`
- [x] 提取路由配置 `src/config/routes.js`
- [x] 创建 `src/components/ui/` 共享组件（10 个组件：StatusBar, Toast, Btn, AppInput, BottomSheet, NavBar, StepDots, LocationPicker, ScrollPicker, BottomNav）
- [x] 创建 `src/components/layout/` PhoneFrame 布局组件
- [x] 创建 `src/context/` 上下文（AppContext, AuthContext, FamilyContext）
- [x] 创建 `src/hooks/` 自定义 Hooks（useToast, useMultiSelect）
- [x] 创建 `src/utils/` 工具函数（china-regions, storage, date）
- [x] 创建 `src/mock/` Mock 数据（family-data, anchors, capsules, notifications）
- [x] 创建 `src/models/` 数据模型（family-member, family-relation, conversation, story-output）
- [x] 创建 `src/main.jsx` + `src/App.jsx` 入口

### Phase 2：核心算法 ✅ 已完成
- [x] 家谱关系图引擎（`src/engine/relation-graph.js`）— buildGraph, BFS最短路径, LCA查找
- [x] 堂/表判定算法（`src/engine/tang-biao.js`）— 纯父系血脉路径判定
- [x] 双向称谓推导（`src/engine/kinship-derive.js`）— 完整6规则（直系/旁系/同辈/姻亲）+ 批量缓存
- [x] 长幼排序（`src/engine/age-compare.js`）— 完整出生日期比较 + 降级策略
- [x] 6 种定位关系转换（`src/engine/label-to-edge.js`）

### Phase 3：页面迁移（进行中）
- [ ] 认证与入伙（P0-P3）— 需从 HTML 提取
- [ ] 创作闭环（P10/P11/P30/P31）
- [ ] 家族与社交（P20/P21/P40/P50）
- [ ] 设置与传承（P51/P52/P60/P61/P62）
- [ ] 内页（12 个子页面，P_NewCapsule 已删除）

### Phase 4：整合与优化
- [x] 路由增强（React 状态路由、history 栈、过渡动画）
- [ ] 样式提取（CSS Modules）— 内联样式已有设计令牌引用
- [ ] TypeScript 迁移（引擎模块优先）
- [ ] Vite 构建配置

---

## 五、已创建文件清单（2026-06-13）

```
src/
├── main.jsx                          # 应用入口（兼容 Vite + Babel Standalone）
├── App.jsx                           # 根组件（路由 + Context + Toast）
│
├── config/
│   ├── tokens.js                     # 设计令牌（C 色彩 + DIMS 尺寸 + RADIUS + SPACE）
│   └── routes.js                     # 32 个页面的路由表 SCREENS
│
├── components/
│   ├── ui/
│   │   ├── StatusBar.jsx             # iOS 状态栏
│   │   ├── Toast.jsx                 # 全局 Toast 提示
│   │   ├── Btn.jsx                   # 按钮（primary/secondary/outline/ghost/warm/text + lg/md/sm/xs）
│   │   ├── AppInput.jsx              # 输入框（label + error + hint + rightEl）
│   │   ├── BottomSheet.jsx           # 底部滑入弹层
│   │   ├── NavBar.jsx                # 顶部导航栏（返回 + 标题）
│   │   ├── StepDots.jsx              # 步骤指示器
│   │   ├── LocationPicker.jsx        # 省市二级选择器（BottomSheet 内嵌）
│   │   ├── ScrollPicker.jsx          # 滚动选择器
│   │   └── BottomNav.jsx             # 底部导航栏（4 Tab + 中央 +）
│   └── layout/
│       └── PhoneFrame.jsx            # 手机外壳（390×844 + Dynamic Island）
│
├── context/
│   ├── AppContext.jsx                # 全局上下文（navigate, showToast, goBack）
│   ├── AuthContext.jsx               # 认证上下文（login/logout + useReducer）
│   └── FamilyContext.jsx             # 家族数据上下文（members/relations/kinship + useReducer）
│
├── hooks/
│   ├── useToast.js                   # Toast 消息管理 Hook
│   └── useMultiSelect.js             # 全选联动规则 Hook（§1.7：多选 + 全选双向同步）
│
├── models/
│   ├── family-member.js              # family_member 表模型 + 工厂函数
│   ├── family-relation.js            # family_relation 表模型 + getParents/Children/Spouse/Siblings
│   ├── conversation.js               # P30 对话消息结构 + 状态追踪模型
│   └── story-output.js               # P31 故事生成输出结构
│
├── engine/
│   ├── relation-graph.js             # 关系图构建（buildGraph）+ BFS最短路径 + LCA查找
│   ├── tang-biao.js                  # 堂/表判定（classifyCousin + getCousinTerm）
│   ├── kinship-derive.js             # 亲属称谓双向推导引擎（deriveKinship + deriveAllKinship）
│   ├── age-compare.js                # 长幼排序（compareAge + classifyBoShu）
│   └── label-to-edge.js              # 6种定位关系→基础边转换 + 性别校验
│
├── mock/
│   ├── family-data.js                # 陈氏一家：10 个成员 + 14 条关系边
│   ├── anchors.js                    # 4 个已点亮锚点 + 3 个灰锚点
│   ├── capsules.js                   # 2 个时间胶囊
│   └── notifications.js             # 5 条通知
│
└── utils/
    ├── china-regions.js              # 中国 34 个省级行政区 + 市级数据 + formatLocation/parseLocation
    ├── storage.js                     # sessionStorage 封装（setCtx/getCtx/clearCtx + 专用键名）
    └── date.js                        # 日期工具（formatDate/daysBetween/daysUntil/lunarToSolar）
```

### 规格落地映射

| 规格文档 | 章节 | 落地文件 |
|---------|------|---------|
| 家谱关系系统 | §2 数据模型 | `models/family-member.js`, `models/family-relation.js` |
| | §4 定位声明 | `engine/label-to-edge.js` |
| | §5 推导算法 | `engine/relation-graph.js`, `engine/kinship-derive.js` |
| | §6 堂表判定 | `engine/tang-biao.js` |
| | §7 长幼排序 | `engine/age-compare.js` |
| | §11 家谱图渲染 | `context/FamilyContext.jsx` |
| Agent 对话 Skill | §8 状态追踪 | `models/conversation.js` |
| | §10 故事输出 | `models/story-output.js` |
| 功能逻辑整理版 | §1.7 全选联动 | `hooks/useMultiSelect.js` |
| | §6.2 仅中国 | `utils/china-regions.js` |

---

*文档持续更新中。*
